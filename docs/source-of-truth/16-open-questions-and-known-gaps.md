# Open Questions and Known Gaps

## Purpose

This register records unresolved architecture evidence, business rules,
implementation mismatches, and technical debt. Status and evidence rules come
from [`00-README.md`](./00-README.md). A recorded gap is not authorization to
change code or the database.

## Open Architecture Questions

| ID | Status | Gap / Question | Impact | Current Evidence | Decision or Verification Required | Blocks Current Work? |
|---|---|---|---|---|---|---|
| INF-001 | UNCERTAIN | Which Git revision, Vercel deployment, Supabase project/branch, and environment set define Production? | A baseline could be mistaken for deployed state. | Local/tracked `main@fd705240`; `develop@5b47f15d`; owner-supplied Vercel evidence names `904d950`. | Reconcile deployment metadata and environment identifiers externally. | No for documentation; yes for claims about live Production. |
| SEC-001 | UNCERTAIN | Do live post-recovery RLS, grants, policies, and the Auth member-creation trigger match historical evidence? | Authorization or onboarding assumptions may be inaccurate. | Runtime code plus `docs/audit/supabase/`; historical package is point-in-time only. | Read-only live catalog/Auth-trigger verification. | No for current documentation; yes before security remediation. |
| POP-001 | PLANNED | Canonical Development participant population is approved but not implemented. | Modules can disagree about current and historical participation. | Current DB definitions repeat population logic; approved direction names `development.cycle_member_population`. | Implement only in a separately approved migration pass. | No for Source of Truth; yes for the population refactor. |

## Business-Rule Questions

| ID | Status | Gap / Question | Impact | Current Evidence | Decision Required | Blocks Current Work? |
|---|---|---|---|---|---|---|
| MAN-001 | UNCERTAIN | What applicability and ranking semantics belong to `manual_adjustment` enrollment? | A canonical population could include or date these members incorrectly. | Enum exists; no current application writer was found. Attendance SQL omits it, Attendance application code treats it as effective-from, and other consumers generally use `effective_from` without a distinct branch. See `03a-development-cycles-and-participation.md`. | Owner-approved semantics and representative cases. | No for documentation; yes for canonical implementation. |
| SUS-001 | UNCERTAIN | What exact temporal interval applies to suspension, and how should history behave after it? | Present status alone cannot establish historical suspension boundaries. | Admin update maps suspended to cycle-member `ineligible`, false ranking eligibility, and null effective end; no confirmed suspension-start field exists. See `03a-development-cycles-and-participation.md`. | Owner-approved temporal semantics and required data source. | No for documentation; yes for complete suspension enforcement. |
| MEM-001 | UNCERTAIN | Is the five-Board-member limit an approved rule? | Endpoint behavior may be mistaken for business policy. | `src/app/api/admin/members/update/route.ts`; no matching verified DB constraint. | Owner confirmation. | No. |

## Implementation / Business-Rule Mismatches

| ID | Status | Verified Mismatch | Impact | Current Evidence | Required Resolution | Blocks Current Work? |
|---|---|---|---|---|---|---|
| REP-001 | TRANSITIONAL | Current Report detail requires active cycle status and ranking eligibility, excludes withdrawn members, and uses stored `effective_from` for every enrollment type; confirmed rules preserve withdrawn history, derive existing-member start from cycle start, and separate participation from ranking. | Legitimate historical Report evidence may disappear, and ranking-ineligible participants may lose Development evidence. | `development.referee_report_detail`; confirmed rules and consumer matrix in `03a-development-cycles-and-participation.md`. | Correct during the approved canonical-population migration and regression-test history. | No for documentation; yes before declaring population semantics complete. |
| REPORT-TIME-001 | CURRENT / UNCERTAIN RULE | Development treats a Report as on time only when LA-converted `submitted_at` has the same date as `kickoff_at::date`; there is no elapsed-hour deadline, and the two timezone-less columns are interpreted differently. | Near-midnight/DST cases can be classified incorrectly, and implementation may not match the intended deadline. | `development.referee_report_detail`; `05-match-reports.md`; `MATCH-TIME-001`. | Confirm the business deadline and canonical timestamp interpretation before changing it. | No for documentation; yes before declaring Report timing authoritative. |
| REPORT-LIFE-001 | CURRENT | Board status API accepts approval or revision from any current source state except that repeated approval is short-circuited. | Unsupported transitions are technically possible without recorded transition history. | `src/app/api/admin/reports/[id]/route.ts`; `05-match-reports.md`. | Approve a legal transition matrix and audit requirements separately. | No. |
| REPORT-AUDIT-001 | CURRENT | Report rows have no approval/review actor timestamps or email-delivery record. | Review history, approval time, retries and delivery outcomes cannot be reconstructed durably. | `public.match_reports` baseline columns and approval/email services. | Decide required audit retention before workflow hardening. | No. |
| REPORT-TXN-001 | CURRENT | Initial submission and revision replacement perform Report/children/assets/status writes as independent statements. | Partial Report versions, deleted child rows, orphan files, or successful writes reported as failures are possible. | Both Report APIs, form cleanup and `05-match-reports.md`. | Design a separately approved transactional/compensation boundary. | No for documentation; yes before consistency is guaranteed. |
| REPORT-ASSIGN-001 | CURRENT | Revision authority combines immutable `submitted_by` with the Match's current Center assignment. | Reassignment after submission can prevent the original Center from correcting a Report without clearly transferring ownership. | Report PATCH, child RLS policies, Match assignment model and `05-match-reports.md`. | Owner decision on reassignment and historical ownership semantics. | No. |
| REPORT-INJ-001 | TRANSITIONAL | `public.report_injuries` is loaded and accepted by revision PATCH, but initial submission/form/email/Competition flows do not provide a complete injury workflow. | The schema capability can be mistaken for a supported product workflow. | Repository baseline, `getMatchForReport()`, Report PATCH and form types. | Confirm whether injuries are current, planned, or residue before changing/removing anything. | No. |
| POP-002 | TRANSITIONAL | Attendance, Quiz, Reports, Evaluations, Development, and Ranking establish participant populations independently. | Invited/inactive/withdrawn cases can diverge between metrics and snapshots. | The consumer matrix in `03a-development-cycles-and-participation.md` traces concrete status, date, enrollment, and ranking-flag differences. | Migrate each consumer to the planned canonical population in dependency order. | No for documentation; yes for the refactor. |
| RNK-001 | TRANSITIONAL | Current ranking refresh upserts serving rows without evidenced stale-row removal. | A member leaving the current population may retain a current ranking row. | Current snapshot refresh function definition in repository DB baseline. | Define and implement safe refresh/reconciliation semantics with explicit approval. | No for documentation; yes before relying on automatic exclusion alone. |
| ATT-001 | TRANSITIONAL | Attendance detail, Admin roster, and record-write validation use different status and enrollment rules. | Invited/ineligible members can accrue records or implicit absences, while manual adjustments can disappear from derived detail. | Comparison matrix in `06-attendance.md`; `getSessionAttendance()`, update-record handler, and `development.referee_attendance_detail`. | Resolve through separately approved canonical-population implementation. | No for documentation; yes before declaring Attendance participation correct. |
| ATT-002 | CURRENT | Admin Attendance legend displays hard-coded `1.0/0.6/0.5/0.0`, while calculation uses per-cycle rules whose schema defaults are `1.0/0.5/0.75/0.0`. | Board users can be shown point semantics that differ from calculated values. | Admin session page, `development.attendance_scoring_rules`, and `referee_attendance_detail`. | Verify live values, then approve a rule-sourced presentation correction. | No for documentation; should be resolved before relying on the legend. |
| MATCH-TIME-001 | CURRENT | `public.matches.kickoff_at` is timezone-less while database and JavaScript consumers apply inconsistent implicit/explicit timezone interpretations. | Upcoming/past state, filters, report timing, evaluation windows, and period assignment can differ near boundaries or DST. | `04-matches.md`; Dashboard/Development views; match parsing/query helpers. | Approve a canonical Los Angeles storage/parsing contract before implementation. | No for documentation; yes before a time-model migration. |
| MATCH-HIST-001 | CURRENT | Referee assignments are mutable columns and no assignment history/crew snapshot was found. | Re-import/reassignment can alter visibility and derived historical evaluation/report evidence. | `public.matches`; import upsert; evaluation/report detail definitions. | Confirm historical crew semantics and approve a preservation model if required. | No for documentation; yes before changing reconciliation. |
| MATCH-CTX-001 | UNCERTAIN | Context-builder failures are logged but do not reject the originating match write. | Matches may persist without context/rosters and be absent from Competition results. | Match trigger/function and import endpoint. | Owner decision on intended partial persistence plus operational verification. | No. |
| COMP-STAT-001 | CURRENT | Standings count only approved reports, while player goal/card views do not filter report status. | Submitted or revision-required events can affect team-profile leaders/discipline before the result counts in standings. | `team_season_standings`, `player_team_season_stats`, and `player_card_reason_stats`; `12-tournaments.md`. | Confirm intended publication boundary, then align only in approved Competition work. | No for documentation; yes before treating all published statistics as final. |
| COMP-RULE-001 | UNCERTAIN | Competition UI states that the top four advance to playoffs, but no supporting database rule or approved business-rule evidence was found. | Presentation may encode an unverified or division-specific rule globally. | `CompetitionCenter.tsx`; no corresponding baseline rule object. | Owner confirmation and scope by competition/division. | No. |
| COMP-UI-001 | CURRENT | The standings Refresh button writes the selected division ID back to the same value and normally does not rerun the effect. | The control appears to refresh but issues no new request. | `CompetitionCenter.tsx`. | Correct in separately approved UI work. | No. |
| COMP-ACT-001 | CURRENT | Organization, season, division, division-season, team, registration, and player activity flags are applied inconsistently by current consumers. | Inactive parent/team/player records may remain visible or contribute depending on the path, and “active competition” has no single enforced meaning. | Competition APIs, context builder, standings/stat views, and Team Profile query; `12-tournaments.md`. | Establish owner-approved visibility/applicability semantics before aligning consumers. | No for documentation; yes before changing lifecycle filtering. |
| STOR-PATH-001 | CURRENT | Uploaded roster paths use mutable external `arbiter_match_id` as the authorization folder and do not validate it as one safe path segment. | Arbiter ID changes leave old paths behind; IDs containing `/` can break policy matching and access. | `buildRosterPath()`, historical Storage policies, and `12a-storage-and-rosters.md`. | Confirm identifier/path invariants and approved migration behavior before changing the contract. | No for documentation; yes before a path redesign. |
| STOR-UI-001 | CURRENT | Generic Match detail loads raw `report_assets`, but `MatchAssets` expects `file_path` image URLs rather than `storage_path` and signed URLs. | That surface cannot reliably render private roster attachments and differs from the current Report attachment UI. | `getMatchDetails()`, `MatchAssets`, and current signed-URL query helpers. | Resolve during separately approved Match/Report UI work. | No. |

## Incomplete or Conflicting Evidence

| ID | Status | Evidence Gap or Conflict | Impact | Current Evidence | Required Evidence | Blocks Current Work? |
|---|---|---|---|---|---|---|
| DEP-001 | UNCERTAIN | Owner-supplied Vercel Production commit is not reconciled with available `main` history. | Cannot make a definitive deployed-revision statement. | Git ancestry and supplied Vercel observation conflict. | Vercel deployment details plus authoritative remote revision mapping. | No for AS-IS repository docs. |
| DB-001 | UNCERTAIN | Historical Supabase audit may differ from the restored live catalog. | Object/security classifications can become stale. | Repository schema baseline and historical audit evidence. | New read-only post-recovery catalog snapshot. | No for implementation facts visible in repo; yes for live-only claims. |
| AUTH-001 | UNCERTAIN | The live caller for `public.handle_new_user()` is not proven by the application-schema trigger snapshot. | Invitation may depend on an externally configured Auth trigger. | Invite handler does not directly insert `public.members`; historical function exists. | Read-only inspection of live `auth.users` triggers. | No for docs; yes before changing onboarding. |

## Technical Debt Requiring Confirmation

| ID | Status | Debt / Limitation | Impact | Current Evidence | Decision or Verification Required | Blocks Current Work? |
|---|---|---|---|---|---|---|
| TYP-001 | CURRENT | Supabase `Database` is aliased to `any` and several local contracts remain loose. | Reduced compile-time protection for schema/nullable changes. | `src/types/database.ts` and current query/component types. | Decide when generated types become authoritative. | No. |
| API-001 | CURRENT | API authorization is handler-specific; `/api/admin` provides no boundary. | A missing guard may expose privileged behavior. | `src/app/api/`; Auth module review. | Continue per-route security audits and verify RLS-dependent endpoints. | No for docs; route-specific before release. |
| ATT-SEC-001 | CURRENT | `GET /api/attendance/session-list` has no authentication/Board guard and calls a service-role roster query using a caller-supplied session ID. | An unauthenticated caller may obtain participant names and attendance statuses for a known/guessed session ID. | Route, `getSessionAttendanceList()`, `getSessionAttendance()`, and `AttendanceSessionDialog`. | Security review and explicit access-policy decision before remediation. | No for documentation; yes before security hardening is complete. |
| MATCH-SEC-001 | CURRENT | `/portal/matches/[match_id]` loads a caller-supplied UUID and report detail without checking assignment; baseline match SELECT RLS allows all authenticated users. | An authenticated member could read another match, crew, report, incidents, asset metadata, and comments. | Match detail page, `getMatchDetails()`, and baseline RLS. | Define intended audience and add defense-in-depth authorization separately. | No for documentation; yes before security hardening is complete. |
| MATCH-SEC-002 | CURRENT | `GET /api/admin/matches` lacks a handler-local Board guard. | Any authenticated caller can retrieve the complete match list under baseline RLS. | Handler, Admin guards, proxy matcher, and baseline RLS. | Standardize Board API authorization after validating consumers. | No for documentation; yes before security hardening is complete. |
| MATCH-SEC-003 | CURRENT | `/api/admin/import-arbiter/preview` has no auth/Board guard and `/api/admin` is outside the proxy matcher. | An unauthenticated caller can make the server parse uploaded spreadsheets; duplicate lookup errors are ignored. The write endpoint does enforce Board. | Preview/import handlers, parser, and proxy. | Add handler-local Board authorization and error handling separately. | No for documentation; yes before security hardening is complete. |
| COMP-SEC-001 | CURRENT | Authenticated Competition APIs use service role and apply no organization/tenant scope to caller-supplied season, division-season, or team-registration IDs. | Any authenticated CAFLA member can read Competition data for every organization represented in the schema; future multi-organization data would not be isolated. | Competition API handlers and schema organization model. | Confirm member-wide visibility intent and any future tenant boundary before remediation. | No for current single-league documentation; yes before claiming tenant isolation. |
| COMP-SEC-002 | NEEDS LIVE VERIFICATION | The browser reads `tournaments.match_roster_view` directly; baseline/historical evidence shows broad authenticated access and a SECURITY DEFINER view without caller filtering. | An authenticated caller with a match UUID may read player names/roster data beyond an intended match scope. | `useMatchRoster()`; baseline grants/view definition; historical Security Advisor evidence. | Read-only live verification of view options, grants, and intended roster audience. | No for documentation; yes before security remediation. |
| COMP-SEC-003 | NEEDS LIVE VERIFICATION | Baseline grants authenticated users EXECUTE on SECURITY DEFINER `tournaments.build_match_context(uuid)`. | A caller may trigger context/roster reconciliation and logs for an arbitrary match, including pre-report roster replacement. | Baseline function body and function grants. | Read-only live function ACL verification and caller search before any change. | No for documentation; yes before security remediation. |
| STOR-SEC-001 | CURRENT | Report create/resubmit APIs accept client-supplied roster `storage_path` values without verifying object existence, ownership, expected match folder, or path/type agreement. | Metadata can point to missing or unrelated objects; Board approval email later downloads stored paths with service role. | Report submit/PATCH routes, form upload flow, email attachment service, and `12a-storage-and-rosters.md`. | Define a server-verifiable object association before remediation. | No for documentation; yes before Storage security hardening is complete. |
| STOR-SEC-002 | NEEDS LIVE VERIFICATION | Repository baseline shows `public.report_assets` DELETE policy `USING (true)`, broader than its select/insert/update policies. | A direct caller may delete asset metadata outside report ownership while leaving the Storage object orphaned. | Baseline RLS/grants and historical policy audit. | Read-only live policy/grant verification, then approve least-privilege behavior. | No for documentation; yes before security remediation. |
| STOR-SEC-003 | NEEDS LIVE VERIFICATION | Historical Storage policies authorize by current center assignment and the first `arbiter_match_id` path folder, not canonical match/report ownership or report state. | Reassignment or external-ID changes alter access independently of immutable report history; lifecycle restrictions are UI-only. | `docs/audit/supabase/16-storage.md`, current path builder, and report form. | Verify live policies and confirm owner-approved access semantics. | No for documentation; yes before security remediation. |
| ROSTER-SEC-001 | NEEDS LIVE VERIFICATION | Browser-accessed `tournaments.match_roster_view` has no assignment predicate; historical evidence shows broad grants and SECURITY DEFINER behavior. | Authenticated members may read player rosters for arbitrary known match IDs. | `useMatchRoster()`, view definition/grants, and `COMP-SEC-002`. | Verify live view options/grants and intended roster audience. | No for documentation; yes before security remediation. |
| REPORT-SEC-001 | CURRENT | Initial submission uses service role after a valid Center check but does not reproduce the client form's full goal/card validation server-side. | A caller can bypass browser validation and persist malformed event semantics within what the database accepts. | `src/app/api/reports/submit/route.ts`, form validator and baseline constraints. | Define server-authoritative validation in a separately approved hardening pass. | No for documentation; yes before security hardening is complete. |
| REPORT-SEC-002 | CURRENT / NEEDS LIVE VERIFICATION | Resubmission checks Report status but has no explicit user/Center/owner comparison and relies on session RLS; reassignment makes the intended actor ambiguous. | Safety depends on live policies and may block the historical submitter or permit an unintended session path. | `src/app/api/reports/[report_id]/route.ts`, baseline RLS and `REPORT-ASSIGN-001`. | Verify live RLS and approve reassignment ownership semantics before remediation. | No for documentation; yes before security hardening is complete. |
| REPORT-SEC-003 | NEEDS LIVE VERIFICATION | Baseline DELETE policies for Report child tables use `USING (true)` while authenticated roles have broad table grants. | Direct callers may delete another Report's child data even though replacement is intended for the owner. | Repository baseline policies/grants and historical security evidence. | Read-only live policy/grant verification before proposing least privilege. | No for documentation; yes before security remediation. |
| REPORT-SEC-004 | CURRENT | Client-provided roster paths are stored as Report assets and later downloaded with service role for approval email. | A forged but accessible path could associate unrelated Storage content with a Report/email. | Report APIs, approval email pipeline, `STOR-SEC-001`, and `05-match-reports.md`. | Resolve with the Storage path-association contract. | No for documentation; yes before security hardening is complete. |
| ATT-LIFE-001 | CURRENT | Single-open-session enforcement is a read-then-update application check with no baseline DB uniqueness constraint. | Concurrent opens can potentially leave multiple open sessions in one cycle. | Open-session handler; Attendance indexes/constraints in repository DB baseline. | Decide whether this invariant requires database enforcement in a separately approved change. | No. |
| MEM-002 | CURRENT | Member/cycle updates are separate and can leave partial state. | Profile and cycle participation may diverge after a failed second write. | Invite/update handlers. | Decide transactional/recovery strategy in Members implementation work. | No. |
| DOC-001 | PLANNED | Source of Truth modules 07–11, 13, and 14 remain placeholders; Match Reports and Storage/Rosters are substantively documented. | Detailed ownership, flows, and limitations for remaining modules are not yet durable. | Corresponding files under `docs/source-of-truth/`. | Complete evidence-based module passes. | No for current documentation work. |

## Resolved Items

| ID | Status | Resolution | Evidence | Resolution Date |
|---|---|---|---|---|
| DOC-000 | CURRENT | Foundation index now distinguishes substantively documented modules from placeholders. | `docs/source-of-truth/00-README.md` | 2026-08-31 |
| ARC-001 | RETIRED | Public Attendance V1, Quiz V1, and Ranking V1 are intentionally retired and must not be restored from historical evidence. | Confirmed recovery/retirement context and current V2 architecture | 2026-08-31 |
